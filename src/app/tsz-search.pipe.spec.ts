import { TiszaSzakiSearchPipe } from './tsz-search.pipe';

describe('TiszaSzakiSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new TiszaSzakiSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
