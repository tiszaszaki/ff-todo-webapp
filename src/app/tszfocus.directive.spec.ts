import { TiszaSzakiFocusDirective } from './tszfocus.directive';

describe('TiszaSzakiFocusDirective', () => {
  it('should create an instance', () => {
    /*
    const directive = new TiszaSzakiFocusDirective();
    expect(directive).toBeTruthy();
    */
  });
});
