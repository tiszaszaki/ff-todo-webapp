import { CurrentRoutingStatus } from './current-routing-status';

describe('CurrentRoutingStatus', () => {
  it('should create an instance', () => {
    expect(new CurrentRoutingStatus()).toBeTruthy();
  });
});
